Read the file `build-score/SKILL.md` in the project root and follow its instructions exactly.
Start by reading all files in `build-score/knowledges/` and checking `requirements/` for existing briefs.
Then ask the user to choose between single-item mode and batch mode.
Conduct the entire conversation in Japanese.
