Read the file `converge/SKILL.md` in the project root and follow its instructions exactly.
Start by reading all files in `converge/knowledges/` and checking `requirements/` for existing briefs.
Then ask the user to choose between single-item mode and batch mode.
Conduct the entire conversation in Japanese.
